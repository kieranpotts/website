<div class="body">
{{!--
  The left nav sidebar ({{> nav}}) is intentionally omitted. This site has no
  versioned releases and only a handful of top-level sections, so all
  navigation is handled by the top header bar (see header-content.hbs). The
  matching nav-toggle (mobile hamburger) is overridden to render nothing.
--}}
{{> main}}
</div>
