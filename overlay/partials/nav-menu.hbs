{{! Navbar menu for kieranpotts.com.

This overrides the theme's default `partials/nav-menu.hbs` (which would otherwise
list one link per Antora component). It is shipped via `supplemental_files` (the
overlay) in the site playbooks, so a supplemental partial of the same path
replaces the one baked into the UI bundle. The theme stays generic; the site's
menu — labels, order, and any external links — lives here.

Internal links are Antora resource IDs resolved with `resolvePage`; external
links are written as plain absolute URLs. }}
<li><a href="{{{relativize (resolvePageURL 'ROOT::index.adoc')}}}">Home</a></li>
<li><a href="{{{relativize (resolvePageURL 'thoughts::index.adoc')}}}">Blog</a></li>
<li><a href="{{{relativize (resolvePageURL 'garden::index.adoc')}}}">Garden</a></li>
<li><a href="{{{relativize (resolvePageURL 'bookmarks::index.adoc')}}}">Bookmarks</a></li>
{{! CV/résumé link — see PLAN.md. Uncomment and set the URL once published:
<li><a href="https://…" rel="noopener">CV</a></li> }}
